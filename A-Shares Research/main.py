from langchain_openai import ChatOpenAI
from langgraph.graph import StateGraph, END
from typing import TypedDict
import akshare as ak
import os
from dotenv import load_dotenv

# 加载环境变量（读取 .env）
load_dotenv()

# ==========================
# 大模型选择model_type 可选：deepseek / qwen / ernie / doubao / spark / hunyuan
# ==========================================
model_type = "model_name"

# 统一配置
model_map = {
    "deepseek": ("deepseek-chat", os.getenv("DEEPSEEK_API_KEY"), os.getenv("DEEPSEEK_BASE_URL")),
    "qwen": ("qwen-turbo", os.getenv("QWEN_API_KEY"), os.getenv("QWEN_BASE_URL")),
    "ernie": ("ernie-4.0", os.getenv("ERNIE_API_KEY"), os.getenv("ERNIE_BASE_URL")),
    "doubao": ("doubao-pro", os.getenv("DOUBAO_API_KEY"), os.getenv("DOUBAO_BASE_URL")),
    "spark": ("spark", os.getenv("SPARK_API_KEY"), os.getenv("SPARK_BASE_URL")),
    "hunyuan": ("hunyuan", os.getenv("HUNYUAN_API_KEY"), os.getenv("HUNYUAN_BASE_URL")),
}

model_name, api_key, base_url = model_map[model_type]

# 初始化大模型
llm = ChatOpenAI(
    model=model_name,
    api_key=api_key,
    base_url=base_url,
    temperature=0.1
)

class State(TypedDict):
    stock_code: str
    data: dict
    tech_report: str
    funda_report: str
    value_report: str
    risk_report: str
    final_report: str
# ------------------------------------------------------------------------------
# 【数据Agent：全真实 A 股数据】
# 股价、PE、PB、财报、行业、估值历史 100% 真实
# ------------------------------------------------------------------------------
# main.py
# 先在文件最上面导入
from tools.data_tool import get_stock_data
from tools.utils import calc_percentile

# ------------------------------------------------------------------------------
# 【数据Agent：调用工具函数获取数据】
# 只负责流程控制，不负责具体数据获取逻辑
# ------------------------------------------------------------------------------
def data_agent(state: State):
    code = state["stock_code"].strip()
    # 直接调用工具函数，一行代码搞定数据获取
    data = get_stock_data(code)
    return {"data": data}

# ------------------------------------------------------------------------------
# 技术Agent
# ------------------------------------------------------------------------------
def tech_agent(state):
    d = state["data"]
    prompt = f"""你是A股资深技术分析师，严格按以下框架输出专业分析（禁止空话，结合数据落地）：
核心数据：当前价格={d['price']}元
分析框架（每一项都要结合价格数据，不遗漏）：
1. 趋势判断：明确是多头/空头/震荡趋势，说明判断依据（无需画图，文字落地）；
2. 量价结构：分析量价匹配度（量价同步/背离），判断当前走势的健康度；
3. 关键价位：结合近期价格波动，给出具体支撑位、压力位（需标注合理区间，不是瞎猜）；
4. 强弱定位：简单对比大盘/行业指数，判断个股相对强弱；
5. 短期信号：给出1-2个核心短期技术信号（如均线排列、MACD状态等）；
6. 结论建议：基于以上分析，给出短期（1-3个交易日）操作参考（简洁，不模棱两可）。
输出要求：分点清晰、逻辑连贯，每个结论都要对应数据支撑，不堆砌专业术语，杜绝“可能、或许”等模糊表述。"""
    return {"tech_report": llm.invoke(prompt).content}
# ------------------------------------------------------------------------------
# 基本面Agent（真实财报）
# ------------------------------------------------------------------------------
def funda_agent(state):
    d = state["data"]
    prompt = f"""你是A股资深财报分析师，严格按以下机构投研框架分析，**所有结论必须基于给出的真实数据，禁止编造未提供的数据，禁止空话**。

核心财务数据（仅基于以下数据分析，未提供的数据不得编造）：
- ROE（净资产收益率）：{d['roe']}%
- 毛利率：{d['gross_profit']}%
- 净利润同比增速：{d['net_profit']}%

【第一步：先判断数据状态】
如果任意核心指标为0、负数或标注"数据异常"，优先输出：
"⚠️ 核心财务数据异常，需重点排查以下风险：
1. 公司可能处于亏损状态或主营业务停滞
2. 财报披露不完整或存在会计调整
3. 持续经营能力存疑"
然后再基于现有数据做补充分析。

【第二步：按框架深度分析（每一项必须结合具体数值）】
1. 盈利质量拆解
   - 基于ROE数值，判断公司盈利驱动模式（高净利率/高周转/高杠杆）
   - 说明当前盈利模式的可持续性和潜在风险
   - 提示：若ROE<5%，通常说明资产盈利能力弱；若ROE>20%且持续，说明有核心壁垒

2. 产品竞争力与成本控制
   - 基于毛利率数值，判断公司产品议价能力和行业地位
   - 对比A股同行业平均水平（给出合理逻辑，无需实际查数据）
   - 分析毛利率变化对净利润的潜在影响

3. 成长能力与韧性
   - 基于净利润增速，判断是趋势性增长/阶段性波动/下滑
   - 说明增速背后的可能驱动因素（量增/价涨/成本下降）
   - 提示：若增速连续下滑，需警惕成长动能枯竭风险

4. A股特有财务风险预警（必须提及）
   - 即使无数据，也要提示需重点排查的A股常见暴雷点：
     ① 应收账款和存货占比过高（可能存在坏账和减值风险）
     ② 商誉占净资产比例过高（可能存在商誉减值风险）
     ③ 大股东股权质押比例过高（可能存在平仓风险）
     ④ 经营现金流与净利润不匹配（可能存在利润造假）

5. 核心结论（必须明确，禁止模棱两可）
   - 明确公司当前盈利状态：【优秀/良好/一般/较差/极差】
   - 总结核心优势（如有）和核心短板
   - 给出明确的基本面风险等级：【低/中/高/极高】
"""
    return {"funda_report": llm.invoke(prompt).content}
# ------------------------------------------------------------------------------
# 估值Agent（真实10年分位）
# ------------------------------------------------------------------------------
def value_agent(state):
    d = state["data"]

    def percentile(hist, cur):
        if not hist: return 0
        arr = sorted(hist)
        cnt = sum(1 for v in arr if v <= cur)
        return round(cnt / len(arr) * 100, 2)

    pe_pct = percentile(d["pe_history"], d["pe_ttm"])
    pb_pct = percentile(d["pb_history"], d["pb"])

    if pe_pct < 20: level = "低估"
    elif pe_pct < 70: level = "合理"
    else: level = "高估"

    prompt = f"""你是A股资深估值分析师，严格按以下机构估值框架分析，**所有结论必须基于给出的真实数据，禁止编造未提供的数据，禁止空话**。

核心估值数据（仅基于以下数据分析）：
- PE(TTM)：{d['pe_ttm']}倍 | 近10年历史分位：{pe_pct}%
- PB(MRQ)：{d['pb']}倍 | 近10年历史分位：{pb_pct}%
- 近10年PE平均值：{d['pe_10_avg']}倍
- 初步估值区间：{level}
- 参考基本面：ROE={d['roe']}%，净利润增速={d['net_profit']}%

【第一步：先判断估值指标有效性（必须优先执行）】
如果出现以下任意一种情况，直接输出估值失效提示，再做补充分析：
1. PE(TTM)≤0 或 PB≤0："⚠️ 估值指标失效！PE/PB为负通常意味着公司亏损或资不抵债，此时分位值无意义，属于典型价值陷阱，而非低估"
2. 分位值=0% 或 分位值=100%："⚠️ 估值分位极端，需结合基本面排查是否存在基本面恶化或泡沫化风险"
3. 公司处于退市整理期或长期停牌："⚠️ 标的已脱离正常交易范畴，所有估值分析均不适用"

【第二步：按框架深度分析（每一项必须结合具体数值）】
1. 历史分位精准解读
   - 分别解读PE和PB分位的核心意义，明确当前估值在历史中的位置
   - 区分不同风格股票的合理分位区间：
     ✅ 消费/价值股：合理分位20%-60%；<20%低估，>70%高估
     ✅ 成长股：合理分位30%-70%；<30%低估，>80%高估
     ✅ 周期股：合理分位40%-60%；<10%底部，>90%顶部（注意：周期股低PE是顶部，高PE是底部）
     ✅ 金融/地产股：合理分位20%-50%；<15%低估，>60%高估
   - 说明当前分位对应的历史胜率（如"PE分位25%，历史上该位置买入持有1年胜率约75%"）

2. 估值-基本面匹配度分析（核心）
   - 计算PEG指标（PEG=PE(TTM)/净利润增速），判断估值与成长的匹配度：
     ✅ PEG<0.8：估值偏低，成长被低估
     ✅ 0.8≤PEG≤1.2：估值合理，与成长匹配
     ✅ PEG>1.2：估值偏高，成长已透支
   - 计算PB-ROE匹配度：判断估值与盈利能力的匹配度（如"ROE30%对应PB6倍，处于合理区间"）
   - 明确指出：当前估值是"高估/合理/低估"，是"基本面驱动"还是"情绪驱动"

3. 估值中枢与安全边际
   - 对比近10年PE平均值，计算当前估值偏离中枢的幅度（如"当前PE28倍，低于10年平均30倍，偏离幅度-6.7%"）
   - 给出量化安全边际：
     ✅ 安全边际>30%：可重仓
     ✅ 安全边际10%-30%：可分批建仓
     ✅ 安全边际0%-10%：观望
     ✅ 安全边际<0%：谨慎持有
   - 说明估值修复的核心前提和潜在催化剂

4. 行业对标与相对估值
   - 结合公司所属行业类型，判断当前估值在行业内的相对位置
   - 说明：与行业平均相比，当前估值是"具备优势"还是"处于劣势"
   - 提示：高估值行业的低估值个股，和低估值行业的高估值个股，都需要重点排查原因

5. A股特有估值风险与机会
   - 风险提示：
     ① 低PE陷阱：周期股顶部、业绩一次性收益、即将暴雷
     ② 高PB陷阱：轻资产公司、商誉过高、资产注水
     ③ 分位陷阱：新股上市时间短、历史数据不足
   - 机会提示：
     ① 错杀低估：基本面无变化，因市场情绪导致的下跌
     ② 业绩反转：估值处于底部，基本面即将改善
     ③ 行业景气向上：估值合理，行业增速超预期

6. 核心结论（必须明确，禁止模棱两可）
   - 明确最终估值评级：【极度低估/低估/合理/高估/极度高估】
   - 给出对应的操作建议：【重仓/分批建仓/观望/减仓/清仓】
   - 总结当前估值的核心优势和核心风险
"""
    rep = llm.invoke(prompt).content
    return {"value_report": rep}

# ------------------------------------------------------------------------------
# 风险Agent
# ------------------------------------------------------------------------------
def risk_agent(state):
    # 第一步：先定义函数！！！
    def percentile(hist, cur):
        if not hist: return 0
        arr = sorted(hist)
        cnt = sum(1 for v in arr if v <= cur)
        return round(cnt / len(arr) * 100, 2)
    # 第二步：再使用函数
    code = state["stock_code"]
    d = state["data"]
    prompt = f"""你是A股资深风险分析师，**只输出基于给定数据的真实可落地风险，绝对禁止“行业周期性风险、市场竞争风险、政策风险”这类通用空话**。

核心个股数据（仅基于以下数据分析，禁止编造未提供的数据）：
- 股票代码：{code}
- 当前价格：{d['price']}元
- PE(TTM)：{d['pe_ttm']}倍 | 估值区间：{"低估" if percentile(d["pe_history"], d["pe_ttm"])<20 else "合理" if percentile(d["pe_history"], d["pe_ttm"])<70 else "高估"}
- ROE：{d['roe']}% | 净利润增速：{d['net_profit']}%

【第一步：异常数据优先处理（必须执行）】
如果出现以下任意一种情况，将对应风险列为第1条最高优先级：
1. 价格=0元或长期停牌："退市风险：标的已脱离正常交易范畴，存在被强制终止上市的可能，将导致本金永久性损失"
2. PE≤0或PB≤0："财务暴雷风险：公司持续亏损或资不抵债，存在净资产为负、被ST/退市的风险"
3. ROE<0且净利润增速<-50%："经营崩溃风险：主营业务大幅亏损，盈利能力持续恶化，持续经营能力存疑"

【第二步：按优先级生成3条核心风险（严格遵守以下规则）】
1. 优先级排序（从高到低）：退市风险 > 财务暴雷风险 > 估值泡沫风险 > 经营恶化风险 > 流动性风险 > 其他风险
2. 每条风险必须包含三要素：**数据依据 + 风险原因 + 具体影响**
3. 绝对禁止通用表述，所有风险必须与该股当前数据一一对应
4. 每条风险控制在1-2句话，简洁有力，重点突出

【A股常见风险模板（仅作参考，必须结合数据改写）】
- 估值类："估值泡沫风险：PE分位85%，处于历史90%以上高估区间，若业绩不及预期，可能引发20%-30%的估值回调"
- 财务类："盈利质量风险：ROE仅3%，远低于行业平均水平，资产盈利能力弱，难以支撑当前估值"
- 经营类："成长失速风险：净利润增速从50%下滑至5%，成长动能枯竭，估值中枢面临下移"
- 流动性类："流动性风险：日均成交额不足1000万元，存在买卖困难、无法及时平仓的风险"
- 减持类："大股东减持风险：近期公告大股东拟减持不超过5%股份，将对股价形成持续压制"

【输出要求】
直接列出3条核心风险，无需多余标题，每条开头标注"1. 2. 3."，按风险严重程度排序。
"""
    # 这里需要复用percentile函数，和估值Agent保持一致
    return {"risk_report": llm.invoke(prompt).content}

# ------------------------------------------------------------------------------
# 首席投研官：最终报告
# ------------------------------------------------------------------------------
def supervisor_agent(state):
    d = state["data"]
    prompt = f"""你是A股头部券商首席投研官，**整合所有专家分析结论，结合行业背景、同业对比、历史趋势、边际变化和情景假设，输出专业、全面、可直接用于投资决策的机构级深度研报**。
禁止简单复制粘贴各部分内容，禁止冗余重复，所有结论必须逻辑自洽、互相呼应，未提供的数据基于A股通用逻辑合理推导，禁止编造具体数值。

核心基础信息：
- 股票代码：{state['stock_code']}
- 实时价格：{d['price']}元

已完成专家分析结论：
1. 估值分析：{state['value_report']}
2. 基本面分析：{state['funda_report']}
3. 技术面分析：{state['tech_report']}
4. 风险提示：{state['risk_report']}

【第一步：异常标的优先处理（必须执行）】
如果出现以下任意一种情况，直接输出【高风险预警报告】，无需按正常框架撰写：
1. 价格=0元或长期停牌：
   输出标题：【⚠️ 高风险预警：标的已脱离正常交易范畴】
   核心结论：该标的存在明确退市风险，所有投资价值分析均不适用，绝对回避
2. PE≤0且ROE≤0：
   输出标题：【⚠️ 高风险预警：财务状况极度恶化】
   核心结论：公司持续亏损，持续经营能力存疑，存在ST/退市风险，绝对回避

【第二步：正常标的研报框架（严格按此顺序输出）】
# 【A股深度投研报告】
## 一、核心结论速览（3句话说清所有重点）
1. 行业与公司定位：[说明公司所属行业及行业地位，如“白酒行业绝对龙头，行业景气度稳健”]
2. 核心投资逻辑：[提炼估值+基本面+技术面的核心结论，如“估值处于历史合理区间，盈利质量优秀，技术面多头趋势”]
3. 综合评级与操作建议：[直接给出最终评级和核心操作建议，如“综合评级7.5/10，建议在支撑位附近分批建仓”]

## 二、行业背景与同业对比
1. 行业景气度：[基于A股行业通用规律，判断当前行业整体景气度（上行/平稳/下行）]
2. 同业对比：
   - 对比行业平均水平，分析公司核心指标的相对位置（如“公司ROE30%，远高于行业平均15%，盈利能力行业领先”）
   - 说明公司在行业中的竞争优势和劣势
3. 共性与个性：[区分是行业共性问题还是公司自身问题，如“行业整体毛利率下滑5%，公司下滑10%，自身成本控制能力弱于行业”]

## 三、估值分析（核心）
提炼估值分析的核心观点，重点说明：
1. 历史分位与趋势：[当前估值在近10年历史中的位置，对比历史最高/最低估值，分析估值变化趋势]
2. 估值-基本面匹配度：[结合ROE和净利润增速，判断估值与盈利的匹配程度]
3. 同业估值对比：[对比行业平均估值，分析公司估值的相对优势/劣势]
4. 安全边际与潜在空间：[计算安全边际，给出合理估值修复/回调空间]

## 四、基本面分析
提炼基本面分析的核心观点，重点说明：
1. 历史趋势对比：[对比公司近3年核心指标变化，判断是趋势性变化还是阶段性波动（如“公司ROE从历史最高25%下滑至15%，盈利能力持续下降”）]
2. 盈利质量与效率：[分析公司的盈利驱动模式、产品议价能力和成本控制能力]
3. 成长能力与韧性：[分析净利润增速的可持续性，判断成长动能是否充足]

## 五、边际变化与潜在催化剂
1. 近期边际变化：[基于A股市场规律，分析可能影响公司基本面的近期事件（如政策调整、行业供需变化、公司重大公告等）]
2. 潜在催化剂：[列出未来3-6个月可能影响股价的核心催化剂（如业绩预告、新品发布、产能释放等）]
3. 潜在利空：[列出未来3-6个月可能影响股价的核心利空因素]

## 六、技术面分析
提炼技术面分析的核心观点，重点说明：
- 当前趋势状态（多头/空头/震荡）
- 关键支撑位和压力位
- 短期技术信号和操作提示

## 七、核心风险提示
直接列出3条最高优先级的核心风险，每条1-2句话，简洁有力。

## 八、情景假设与综合评级
### 情景假设（分三种情况评估）
1. **乐观情景**：[假设所有利好因素兑现，给出对应的估值区间和股价目标]
2. **中性情景**：[假设基本面无重大变化，给出对应的估值区间和股价目标]
3. **悲观情景**：[假设所有利空因素兑现，给出对应的估值区间和股价目标]

### 综合评级：X/10分
评分标准（严格执行）：
- 9-10分：极度低估+基本面优秀+无重大风险+明确催化剂，强烈推荐
- 7-8分：估值合理+基本面良好+风险可控+潜在催化剂，推荐
- 5-6分：估值中性+基本面一般+有一定风险，观望
- 3-4分：估值偏高+基本面较差+风险较大，减仓
- 1-2分：极度高估+基本面恶化+重大风险，清仓

### 操作参考（分情况给出明确建议）
- 未持仓：[如“可在XX-XX元区间分批建仓，总仓位不超过30%”]
- 已持仓：[如“继续持有，跌破XX元止损，目标价XX-XX元”]
- 止损位：[明确给出具体止损价位]
- 目标价：[结合中性情景，给出合理目标价区间]

---
**免责声明**：本报告基于公开数据整理，仅供投资参考，不构成任何投资建议。股市有风险，入市需谨慎。
"""
    final = llm.invoke(prompt).content
    return {"final_report": final}

# ------------------------------------------------------------------------------
# 多Agent流程构建
# ------------------------------------------------------------------------------
wf = StateGraph(State)
wf.add_node("data", data_agent)
wf.add_node("tech", tech_agent)
wf.add_node("funda", funda_agent)
wf.add_node("value", value_agent)
wf.add_node("risk", risk_agent)
wf.add_node("supervisor", supervisor_agent)

wf.set_entry_point("data")
wf.add_edge("data", "tech")
wf.add_edge("data", "funda")
wf.add_edge("data", "value")
wf.add_edge("data", "risk")
wf.add_edge(["tech","funda","value","risk"], "supervisor")
wf.add_edge("supervisor", END)

app = wf.compile()

# ------------------------------------------------------------------------------
# 运行
# ------------------------------------------------------------------------------
if __name__ == "__main__":
    code = input("请输入股票代码：")
    res = app.invoke({"stock_code": code})
    print("\n" + res["final_report"])